"""
file_service.py: NOT part of Phase 1.

File-level operations beyond "open with default app" (e.g. move/copy/
delete from within the launcher, drag-and-drop handling) are not in
the agreed Phase 1 feature list. core/command_runner.py covers the one
file action Phase 1 needs (launch). This file is a structural
placeholder matching the target architecture.
"""
